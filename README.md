# API-Cloud-Aata-Pipeline
An end-to-end AWS-ready data pipeline that ingests REST API data, performs ETL processing, stores raw &amp; curated datasets in a data lake (S3 simulation), and enables analytics using SQL (Athena) and BI dashboards (QuickSight-ready). Built using Python, Pandas, and AWS concepts.
